{% for message in app.flashes('success') %}
    <div class="notyf-message" data-name="success">
        {{message}}
    </div>
{% endfor %}

{% for message in app.flashes('info') %}
    <div class="notyf-message" data-name="info">
        {{message}}
    </div>
{% endfor %}

{% for message in app.flashes('warning') %}
    <div class="notyf-message" data-name="warning">
        {{message}}
    </div>
{% endfor %}

{% for message in app.flashes('error') %}
    <div class="notyf-message" data-name="error">
        {{message}}
    </div>
{% endfor %}